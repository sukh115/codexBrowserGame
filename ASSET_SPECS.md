# ASSET_SPECS.md — 디자이너 · 사운드 전달용 에셋 규격서

개발은 플레이스홀더로 먼저 진행 중이라, 아래 규격만 맞으면 **파일을 폴더에 넣는 것만으로 게임에 적용**됩니다.
파일명은 반드시 아래 표기 그대로 사용해주세요. 테마·스타일은 자유입니다.

## 디자이너

### 1. 3D 캐릭터 (오버월드용) — 우선순위 1
| 항목 | 규격 |
|---|---|
| 파일 | `character.glb` |
| 용량 | 2MB 이하 (Draco 압축 전 기준 폴리곤 1~2만 이내 권장) |
| 애니메이션 | 클립 2개, 이름 고정: `idle`, `walk` (각 1~2초 루프) |
| 비고 | 텍스처는 glb에 임베드, 1024px 이하. 리깅이 어려우면 무애니메이션 모델만 줘도 됨(코드로 바운스 처리) |

제작 팁: Blender에서 Mixamo 리깅도 OK. 스타일은 단순할수록 좋음(참고: 춘식이 관찰일기의 토이 느낌).

### 2. 오버월드 그래픽 — 우선순위 3
| 항목 | 규격 |
|---|---|
| 바닥 | `ground.webp` — 2048×2048, 심리스 아니어도 됨(한 장을 통으로 깔음) |
| 지역 입구 | `entrance.webp` — 512×512, 투명 배경 (빌보드로 세움) |
| 소품(선택) | `prop_1.webp` ~ `prop_5.webp` — 512×512 투명 배경, 빌보드 |

바닥 한 장이 곧 맵 전체입니다. 손그림 지도처럼 길·마을·입구 위치를 그려주면 그대로 월드가 됩니다.
입구 위치는 개발자와 좌표만 맞추면 됩니다.

### 3. 2D 지역 배경 (가장 중요한 그림) — 우선순위 2
| 항목 | 규격 |
|---|---|
| 파일 | `region_bg.webp` |
| 해상도 | 4096×2048 (가로로 긴 한 장) |
| 용량 | 1.5MB 이하 (webp 품질 80 정도) |

- "윌리를 찾아라"처럼 구경거리가 빽빽한 그림일수록 좋습니다 (숨은 음표를 박을 자리가 필요)
- **음표 4개를 숨길 위치**를 그림에 자연스럽게 마련해주세요 (그림 속에 직접 그리지는 말 것 —
  음표는 게임이 얹습니다. "이 4곳에 숨기면 좋겠다"를 좌표 메모로 전달)
- 미니게임 입구가 될 오브젝트 3개(문/천막/상자 등 눈에 띄는 것)를 그림에 포함

### 4. UI — 우선순위 4
| 항목 | 규격 |
|---|---|
| 음표 아이콘 | `note.webp` — 256×256 투명 배경 |
| 완료 표시 | `done.webp` — 256×256 투명 배경 |
| 로고/타이틀(선택) | `title.webp` — 1024×512 투명 배경 |

### 5. 온실 화분 조각 (선택 납품)

| 항목 | 규격 |
|---|---|
| 파일 | `pot_piece_1.webp` ~ `pot_piece_4.webp` |
| 해상도 | 각 512×512px |
| 배경 | 투명 배경 |
| 여백 | 화분 조각 외곽의 투명 여백을 최소화 |

파일이 없으면 게임이 캔버스로 흙색 조각을 절차 생성합니다. 납품 후 `regionData/neonForest.ts`의 `pieceImagePaths`만 채우면 교체됩니다.

## 사운드

### 1. 지역 곡 스템 — 이 게임의 핵심입니다
| 항목 | 규격 |
|---|---|
| 파일 | `stem_1.mp3` ~ `stem_4.mp3` (4트랙 기준, 늘려도 됨 — 개발자와 개수만 합의) |
| 형식 | mp3 192kbps 이상 또는 ogg. 44.1kHz |
| 길이 | **전 트랙 완전히 동일한 길이** (같은 마디 수, 같은 BPM). 8~16마디 루프 권장 |
| 구성 예 | 1=리듬, 2=베이스, 3=화음/패드, 4=멜로디 (쌓이는 순서대로 번호) |

반드시 지켜야 하는 것:
- 4파일을 같은 프로젝트에서 **같은 구간을 트랙별로 뮤트 솔로 내보내기** 한 것이어야 함
  (시작점이 1샘플이라도 어긋나면 게임에서 어긋난 채 겹쳐 들립니다)
- 루프 경계가 자연스럽게 이어질 것 (꼬리 리버브가 잘리지 않게 마디 안에서 정리)
- 1번(리듬)만 단독으로 들어도 심심하지 않게 — 게임 초반엔 이것만 들립니다
- **반드시 자작 또는 AI 생성 오리지널 곡** (기존 곡 사용 불가 — 출품 규정/저작권)
- BPM 값을 개발자에게 알려줄 것 (리듬 미니게임 동기화에 사용)

### 2. 효과음
| 파일 | 용도 |
|---|---|
| `sfx_tap.mp3` | 버튼/바닥 탭 |
| `sfx_found.mp3` | 음표 발견 (밝고 짧게, 0.5초 내) |
| `sfx_clear.mp3` | 미니게임 클리어 |
| `sfx_enter.mp3` | 지역 진입 전환 |
| `sfx_complete.mp3` | 노래 완성 (3~5초, 화려하게) |

### 3. 음표 7단계 음악 변화 가이드

현재 프로토타입은 음표의 위치와 음악 변화를 고정 연결합니다. 어느 음표를 먼저 찾느냐에 따라
조합 순서가 달라지는 것이 의도이며, 실제 스템도 아래 역할이 구분되게 제작해주세요.

| 음표 | 프로토타입 변화 | 실제 음원 제작 의도 |
|---|---|---|
| 1 | 리듬 스템 켜기 | 곡의 출발점. 단독으로도 그루브가 느껴질 것 |
| 2 | 리듬 약 28% 강화 | 퍼커션 또는 리듬 디테일이 추가된 느낌 |
| 3 | 베이스 스템 켜기 | 리듬과 결합했을 때 저역과 진행감 보강 |
| 4 | 리듬 스템 켜기 | 리듬 미니게임의 드럼 연주가 곡으로 이어짐 |
| 5 | 멜로디 스템 켜기 | 기억하기의 선율이 곡의 대표 멜로디로 이어짐 |
| 6 | 화음 스템 + 로우패스 개방 | 조율된 화음과 함께 공간이 밝고 선명해짐 |
| 7 | 필터 완전 개방 + 약 16% 부스트 | 완성 순간. 전체 스템의 정점이 분명할 것 |

위 수치는 최종 믹스 지시가 아니라 게임 내 변화량을 검증하기 위한 가이드입니다. 실제 파일을
받은 뒤 스템별 체감 음량과 필터 범위는 통합 단계에서 다시 조정합니다.

## 전달 방법 / 마감

- 도착 순서 제안: 스템(초안이라도) → 지역 배경 → 캐릭터 → 나머지
  (스템은 게임플레이 검증에, 배경은 좌표 작업에 먼저 필요합니다)
- 중간본 환영: 완성본 전에 러프라도 먼저 주면 그걸로 통합 테스트를 돌립니다
- 최종 마감: 제출 3일 전 (통합 + 폴리시 이틀 + 버퍼 하루)
- 전체 에셋 용량 합계 목표: 4MB 이하 (초기 로딩 3초 목표 때문. 초과 시 개발자가 압축 조정)
