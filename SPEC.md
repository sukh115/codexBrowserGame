# SPEC.md — 게임 기술 스펙 v1

테마/아트/음악의 내용은 미정이며 이 스펙과 무관하다. 여기서는 동작만 정의한다.

## 1. 전체 흐름

```
[로딩 화면] → [3D 오버월드] ⇄ [2D 지역] → [완성 연출]
```

1. 로딩 화면: 에셋 로드 진행률 표시 → 완료 시 "시작" 버튼 (첫 제스처 = 오디오 unlock)
2. 3D 오버월드: 클릭 이동. 지역 입구에 도달하면 "들어가기" 프롬프트 → 2D 지역으로 전환
3. 2D 지역: 음표 수집 (숨은 음표 찾기 + 미니게임). 언제든 오버월드로 나갈 수 있다
4. 음표 N개(기본 7개)를 모두 모으면 완성 연출: 완성곡 풀 재생 + 축하 연출

## 2. 3D 오버월드

- 카메라: 쿼터뷰 느낌의 PerspectiveCamera. 캐릭터를 부드럽게 팔로우(lerp).
- 이동: 바닥 클릭/탭 → Raycaster로 지점 계산 → 캐릭터가 그 지점으로 걸어감.
  - 목적지 마커 표시(짧게 반짝이고 사라짐)
  - 이동 속도 일정. 도착 판정 반경 0.3 유닛
- 맵: 단일 바닥 평면(플레이스홀더: 격자 텍스처) + 외곽 이동 제한(사각 경계)
- 지역 입구: 맵에 1개. 반경 2유닛 진입 시 HUD에 "들어가기" 버튼 표시,
  이탈 시 숨김. 버튼 탭 → 씬 전환
- 씬 전환 연출: GSAP로 카메라가 입구를 향해 줌인 + 화면 페이드(0.8초) → 2D 씬

## 3. 2D 지역

OrthographicCamera + 대형 배경 일러스트 1장(스프라이트 평면) 기반.

- 배경: 가로로 긴 일러스트 (기준 4096×2048). 드래그로 좌우 팬, 휠/핀치로 줌(1x~2.5x)
- 카메라 경계: 배경 밖이 보이지 않게 클램프

### 3-1. 숨은 음표 찾기 (윌리를 찾아라 방식) — 음표 4개

- 배경 곳곳에 작은 음표 오브젝트가 숨겨져 있다 (JSON 좌표 데이터로 정의)
- 탭 판정 반경: 화면 기준 24px. 성공 시: 반짝 연출 + 효과음 + 음표 획득
- 힌트: 60초 이상 못 찾으면 해당 음표 근처가 은은하게 파동(1회)
- 찾은 음표는 배경에서 사라지고 HUD 카운터에 등록

### 3-2. 미니게임 — 음표 3개 (각 1개씩)

지역 내 오브젝트(플레이스홀더: 문 아이콘 3개)를 탭하면 미니게임 모달이 열린다.
미니게임은 전부 DOM+Canvas 오버레이로 만들고 게임 씬과 분리한다.

1. **타이밍 바**: 움직이는 바를 목표 구간에서 탭. 3회 성공 시 클리어
2. **리듬 탭**: 흘러오는 노트를 박자에 맞춰 탭. 정확도 70% 이상 클리어
   (현재 재생 중인 스템의 BPM과 동기화 — StemPlayer에서 BPM을 가져온다)
3. **소리 기억하기**: 4개 버튼이 순서대로 소리를 냄 → 같은 순서로 입력. 3라운드

- 실패 시 즉시 재도전 가능 (페널티 없음)
- 클리어한 미니게임은 완료 표시로 바뀐다

## 4. 스템 오디오 시스템 (게임의 핵심)

- 지역 곡은 스템 K개(기본 4: 리듬/베이스/화음/멜로디)로 구성. 전 스템 동일 길이 루프
- 지역 진입 시 모든 스템을 **동시에 start()**, 전부 gain 0
- 음표를 획득할 때마다 정해진 순서로 스템 1개의 gain을 0→1 페이드(2초)
  - 음표 7개 : 스템 4개 → 매핑 테이블로 정의 (음표 2개당 스템 1개 + 마지막은 전체 볼륨 부스트 등)
  - 매핑은 `assetManifest.ts`에 데이터로 정의, 코드에 하드코딩 금지
- 루프 경계에서 틈이 없어야 한다: AudioBufferSourceNode.loop = true 사용
- 오버월드로 나가면 현재 켜진 스템 상태가 유지된 채 볼륨만 낮춰 계속 재생(0.3배)
- 완성 시: 모든 스템 on + 마스터 필터 해제(로우패스 열기) 연출 + 완성곡 1루프 재생

플레이스홀더: 스템 4개를 오실레이터로 코드 생성 (예: 킥 패턴 / 베이스 라인 /
패드 화음 / 리드 멜로디, 120BPM 4마디). 실제 음원 도착 시 파일 교체만으로 동작해야 한다.

## 5. 상태와 저장

- store 상태: `collectedNotes: string[]`, `clearedMinigames: string[]`,
  `currentScene`, `muted`, `completed`
- localStorage에 자동 저장/복원. "처음부터" 버튼 제공
- 완성 후 재방문 시: 완성곡이 켜진 상태로 지역 탐험 가능

## 6. HUD / UI

- 좌상단: 음표 카운터 `♪ 3/7` (획득 시 펄스 애니메이션)
- 우상단: 음소거 토글, (지역 내에서) 나가기 버튼
- 하단 중앙: 컨텍스트 버튼("들어가기" 등)
- 완성 연출: 카운터가 중앙으로 확대 → 파티클 + 완성곡
- 전부 DOM. 게임 캔버스 위 오버레이 레이어

## 7. 완성(엔딩) 연출

1. 마지막 음표 획득 → 입력 잠금 2초
2. 화면 비네트 밝아짐 + 파티클 + 카메라 슬로우 줌아웃
3. 완성곡 풀 볼륨 재생, "OO의 노래가 완성되었어요" 타이틀
4. 이후 자유 탐험 모드

## 8. 우선순위 (시간 부족 시 자르는 순서)

컷 1순위: 미니게임 3(소리 기억하기) → 2순위: 힌트 시스템 → 3순위: 저장 기능
절대 컷 금지: 스템 쌓이는 연출, 3D↔2D 전환, 숨은 음표 찾기
