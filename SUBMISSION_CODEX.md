# Codex와 함께 게임을 만든 과정

## 신청서 붙여넣기용

이 프로젝트에서는 Codex를 단순 코드 생성기가 아니라 구현·검증·개선 작업을 함께 수행하는 개발 파트너로 활용했습니다. 먼저 게임의 핵심 규칙과 기술 제약을 `SPEC.md`와 `AGENTS.md`에 명시하고, Codex가 매 작업마다 이를 기준으로 기능을 구현하도록 했습니다. 개발은 기능 단위로 나누고 TypeScript strict 검사, 프로덕션 빌드, 브라우저 QA, Git 커밋을 반복하는 방식으로 진행했습니다.

Codex는 Three.js 기반 3D 오버월드와 2D 악기점 전환, 클릭·드래그 이동, 숨은 음표 수집, 앰프·페달·CRT 미니게임, 진행도 저장·초기화, NPC 조건별 대사와 완료 연출을 구현하는 데 사용했습니다. 특히 Web Audio API로 동일한 재생 시각과 루프 위치를 공유하는 3레이어 스템 시스템을 구성하고, 음표 진행도에 따라 각 레이어의 볼륨과 필터가 자연스럽게 변화하도록 구현했습니다. 실제 에셋이 늦게 도착해도 개발을 계속할 수 있도록 코드 생성 플레이스홀더와 `assetManifest` 중심의 교체 구조도 만들었습니다.

문제 해결 과정에서도 Codex를 적극 활용했습니다. 3D↔2D 장면 왕복 시 이벤트 중복과 메모리 누수를 점검해 Three.js 리소스의 `dispose()` 경로를 보완했고, 씬 페이드 중 중복 입력, 초기화 후 사운드 잔류, 저장소 쓰기 예외, 음소거 복원, 핀치 종료 후 드래그 단절 등을 재현하고 수정했습니다. 모바일 세로 화면에서는 HUD와 버튼의 안전 영역, 44px 이상 터치 영역, 배경 팬·핀치 줌 경계와 가로 저해상도 버튼 겹침을 반복 점검했습니다. 마지막으로 7/7 전체 플레이, 재도전과 초기화, 새로고침 복원, 장면 왕복 10회, 콘솔 오류와 빌드 크기를 확인하고 GitHub Pages에 자동 배포했습니다.

사람이 직접 결정한 부분도 분명히 구분했습니다. 게임의 주제와 핵심 재미, 3D 탐색에서 2D 악기점으로 이어지는 흐름, 음표와 음악 레이어의 관계, 다카포와 피네의 서사, 대사 톤, 미니게임 구성과 난이도는 팀이 직접 기획하고 조정했습니다. 디자이너는 악기점 배경과 NPC 이미지를, 사운드 디자이너는 동기화된 음악 스템과 효과음을 제작했습니다. Codex는 이 기획과 에셋을 브라우저 게임으로 통합하고 반복 QA를 지원했으며, 최종 선택과 시각·음악적 판단은 팀이 실제 플레이를 통해 결정했습니다.

## 입력 전 확인

- 신청서에는 위의 `신청서 붙여넣기용` 본문만 붙여넣습니다.
- 기존 프로젝트를 활용한 경우에는 챌린지 이전 작업과 기간 중 새로 개발한 범위를 별도로 덧붙입니다.
- 글자 수 제한이 입력 화면에서 추가로 표시되면 핵심 내용을 유지한 축약본을 사용합니다.
